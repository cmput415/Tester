procedure main() returns integer {
    character[*] v = ['a', '8', '(', '$', 'O', '1'];
    character[*] u = reverse(v);

    u -> std_output;
    '\n' -> std_output;

    return 0;
}
CHECK:[1 O $ ( 8 a]
